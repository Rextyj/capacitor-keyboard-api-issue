import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'capacitor-keyboard-api-issue',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
